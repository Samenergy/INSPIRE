import React from 'react';
import StaticDocumentsNew from './StaticDocumentsNew';

// This component will be the default export
// It uses the static version by default, but you can replace it with AnimatedDocuments
// once framer-motion is installed
const Documents = StaticDocumentsNew;

export default Documents;